# GLFW
Basic setting for GLFW project using cmake

_To build and run project in command line_
```shell
mkdir vscode-build
cd vscode-build
cmake ..
cmake --build .
Debug/Tutorial.exe
```
_To run tests_
```shell
ctest -N
# or
ctest -C
# or
ctest -C Debug -vv
```
